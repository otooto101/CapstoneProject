@model LifeAdvisor.Web.Models.LoginViewModel
@{
    Layout = "_Layout";
    ViewData["Title"] = "Login";
}

<div class="auth-wrapper">
    <div class="auth-watermark"></div>
    <div class="auth-card">

        <div class="auth-header-block">
            <div class="auth-logo auth-logo--stacked">
                <img src="/images/logo.png" alt="LifeAdvisor Logo" class="auth-logo-img" />
                <div class="brand-lockup">
                    <span class="brand-text">LifeAdvisor</span>
                    <span class="brand-tagline">digital twin guidance</span>
                </div>
            </div>

            <div class="auth-header-divider">
                <span></span>
            </div>

            <div class="auth-heading-group">
                <h2 class="auth-title">Welcome back</h2>
                <p class="auth-subtitle">Sign in to your account</p>
            </div>
        </div>

        @if (!ViewData.ModelState.IsValid)
        {
            <div class="auth-error">
                @Html.ValidationSummary(false)
            </div>
        }

        <form method="post">
            @Html.AntiForgeryToken()

            <div class="auth-field">
                <label asp-for="Email">Email</label>
                <input asp-for="Email" class="auth-input" placeholder="you@example.com" />
                <span asp-validation-for="Email" class="validation-message"></span>
            </div>

            <div class="auth-field">
                <label asp-for="Password">Password</label>
                <div class="auth-input-wrap">
                    <input asp-for="Password" id="password" class="auth-input" type="password" placeholder="Your password" />
                    <button type="button" class="auth-eye" onclick="togglePassword()">👁</button>
                </div>
                <span asp-validation-for="Password" class="validation-message"></span>
            </div>

            <button type="submit" class="auth-btn" data-loading-text="Signing you in…">Sign in</button>
        </form>

        <p class="auth-switch">
            Don't have an account? <a href="/Auth/Register">Create one</a>
        </p>
    </div>
</div>

<partial name="_BrainBuddy" />

<script>
    function togglePassword() {
        const input = document.getElementById('password');
        input.type = input.type === 'password' ? 'text' : 'password';
    }
</script>
